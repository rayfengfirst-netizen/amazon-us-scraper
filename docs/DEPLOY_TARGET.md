# 发布目标约定

## 默认目标（当前）

- 服务器：`8.221.99.46`
- SSH：`root@8.221.99.46`
- 应用目录：`/opt/amazon-us-scraper`
- 服务名：`amazon-us-scraper`
- 本机监听：`127.0.0.1:8989`

## 操作约束

1. 后续默认发布都走 `8.221.99.46`。  
2. 如需临时发布到其他机器，必须先在任务说明中明确。  
3. 不自动修改线上 `.env`；仅拉代码、装依赖、重启服务。  
